#include<stdio.h>
int main(){
   int num,range,i,x,result;
    printf("Enter range =");
   scanf("%d",&range);
   for(i=1;i<=range;i++){
      printf("Enter Digit =");
        scanf("%d",&num);
   for(x=1;x<=range;x++){
      result = (range*num)+
   }
   }
   return 0;
}